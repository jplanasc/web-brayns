/**
 * A session is eveything is currently loaded in Brayns.
 * It is essentially a list of models with their transformations.
 * But it can also be environment map, camera and renderer settings, etc...
 */

export default { getSnapshot }


/**
 * Creates a session object from the current Brayns state.
 */
function getSnapshot() {

}
